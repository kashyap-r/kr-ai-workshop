{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run TypeScript file",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/src/hello.ts",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}