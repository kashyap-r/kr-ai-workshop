
How the pipeline works ?


                         Raw Corpus
                             │
                             ▼
                    FilesystemConnector
                             │
                             ▼
                      SourceDocument
                       │           │
                       │           └── metadata
                       ▼
                         Parser
                             │
                             ▼
                      ParsedDocument
                       │           │
                       │           ├── metadata
                       │           │
                       ▼           ▼
                 parsed/*.txt   JSONL registry
                       │
                       │
                       └──────┐
                              ▼
                     structured logging
                              │
                              ▼
                    logs/ingestion.log
                              │
                              ▼
                          Chunking
                              │
                              ▼
                    logs/Chunking.log


Delete the exisitn data first 

rm -rf data/processed/parsed/ZCompanyLLC
rm -rf data/processed/chunks/ZCompanyLLC
rm -f data/processed/metadata/parsed_documents.jsonl
rm -f logs/ingestion.log
rm -f logs/chunking.log

Step 1: Run:
python scripts/ingest_documents.py
OR 
uv run python scripts/ingest_documents.py

Check:
data/processed/parsed/ZCompanyLLC/
data/processed/metadata/parsed_documents.jsonl
logs/ingestion.log

Step 2: Run 
uv run python scripts/chunk_documents.py

Check: 
data/processed/chunks/ZCompanyLLC/
logs/chunking.log

Step 3: To create the embeddings and vector store and persist the embeddings
Run 
uv run --project python python scripts/3.embed_and_index.py


Step 4: Query test 
uv run --project python python scripts/4.query_vector_store.py



At this point the end-to-end retrieval path: 
Raw HR corpus
    ↓
FilesystemConnector
    ↓
Parser
    ↓
ParsedDocument
    ↓
RecursiveChunker
    ↓
DocumentChunk
    ↓
BGE-small
    ↓
384-dim embeddings
    ↓
ChromaVectorStore
    ↓
Query embedding
    ↓
Top-K retrieval
    ↓
RetrievalResult


Step 5: Indexing 
Run: uv run python scripts/index_documents.py
Note the output, it should say Indexing complete: XX chunks indexed

Runt the command agaian if you want to check if running the script creates duplicate indexes.

Step 6: Run the retrieval test 
Run: uv run python scripts/4.query_vector_store.py
We should see sensible results w.r.t to the question asked!!


At this point, M9 is complete i.e. Re-ranking is complete 
To test E2E 

Start the API, In the terminal, Run:  
uv run uvicorn rag_platform.api:app --reload

Then in another Terminal, Run: 
curl -X POST http://127.0.0.1:8000/query \
  -H "Content-Type: application/json" \
  -d '{"query":"What is the company'\''s policy for working from home?","top_k":5}'

Expected Result: You should get a result as below 
POST /query
   │
   ▼
M7 Query Understanding
   │
   ▼
M8 Hybrid Retrieval
   │
   │ 20 candidates
   ▼
M9 Cross-Encoder Reranking
   │
   │ top 5
   ▼
API Response
result_count = 5
latency_ms   = 161.38


