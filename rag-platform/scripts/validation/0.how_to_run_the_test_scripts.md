scripts/
└── validation/
    ├── m7_query_understanding_validation.py
    └── m8_hybrid_retrieval_validation.py

 scripts/
└── validation/
    ├── m7_query_understanding_validation.py
    ├── m8_hybrid_retrieval_validation.py
    ├── m9_reranking_validation.py
    ├── m10_context_assembly_validation.py
    ├── m11_generation_validation.py
    └── m12_citations_validation.py
    
For M8, I actually want more than one script eventually.

There are two levels:
Feature validation that tests BM25 independently.
scripts/validation/
└── m8_bm25_validation.py

Run
uv run python scripts/validation/m8_bm25_validation.py


Complete M8 validation
scripts/validation/
└── m8_hybrid_retrieval_validation.py
Tests the below:
Dense
  +
BM25
  ↓
RRF
  ↓
Hybrid
Run: 
uv run python scripts/validation/m8_bm25_validation.py


| Test                   | Query               | Result                            | Status |
| ---------------------- | ------------------- | --------------------------------- | ------ |
| FV-01 Exact identifier | `EMP-1042`          | `chunk-1`, rank 1                 | ✅      |
| FV-02 Multi-term       | `working from home` | `chunk-3`, rank 1, score `1.9906` | ✅      |

1. Identifier-aware retrieval

EMP-1042 → chunk-1

This validates our tokenizer's handling of identifiers such as EMP-1042.

2. Semantic-independent keyword retrieval

working from home → chunk-3

BM25 is responding to lexical term overlap and term frequency rather than embeddings. That's exactly the complementary behavior we need alongside dense retrieval.




TEst Approach 
1. Unit tests
uv run pytest tests/retrieval/test_sparse.py

Tests individual implementation behavior.

2. Module functional validation
uv run python scripts/validation/m8_bm25_validation.py

Demonstrates the feature as a user/developer would actually exercise it.

3. Full regression
uv run pytest

Makes sure M8 didn't break M1–M7.

### So.. far in M8.2 

Query
  │
  └── BM25Retriever
          │
          └── ranked candidates

### M8.3 

                         ┌── DenseRetriever ──┐
                         │                    │
Query ───────────────────┤                    ├── RRF ──> HybridRetriever
                         │                    │
                         └── BM25Retriever ───┘


The important architectural point is:

Dense and sparse retrieval remain independent retrieval strategies. HybridRetriever orchestrates them; RRF combines their rankings.

Test the E2E Hybrid Retrieval and this will use:

1. actual BGE embedding model
2. actual Chroma vector store
3. actual persisted BM25 index
4. actual HybridRetriever
5. actual RRFFusion
6. actual ZCompanyLLC corpus


uv run python scripts/validation/m8_hybrid_retrieval_validation.py


To rebuild Chroma vector index + BM25 persistent index + index_manifest.json

uv run python scripts/5.0.index_documents.py --rebuild


### Validate Context Assembly 
uv run python scripts/validation/m10_context_assembly_validation.py

Refer to the Test script 10.3 in the conversation. There are a few more  pending. 

