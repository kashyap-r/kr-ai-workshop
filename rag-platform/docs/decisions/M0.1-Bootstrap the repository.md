1. Start with the below directory structure

ent-rag-platform/
├── python/
├── typescript/
├── data/
├── configs/
├── docs/
└── tests/


2. Build the python environment as 
Python 3.x
pyproject.toml
virtual environment
pytest
typing
linting
formatting

Commands to run: 

cd rag-platform/python

uv --version
uv python list
uv python install 3.12
uv venv --python 3.12
source .venv/bin/activate

python --version
which python

uv sync --extra dev

pytest
ruff check .
mypy src



3. For TypeScript, Setup the following.. 
Node.js
TypeScript
package.json
tsconfig.json
Vitest
ESLint
Prettier

Commands to run:
cd ../typescript

node --version
npm --version

npm install
npm test
npm run build
npm run lint
npm run format

4. Set up the shared engineering conventions
.gitignore
.env.example
README
logging conventions
error conventions
configuration conventions

Our Working Rhythm 
1. What are we building?
2. Why does this component exist?
3. Architecture
4. Design decisions
5. Production-quality contracts
6. Implementation — Demo A
7. Implementation — Demo B
8. Tests
9. Experiment / benchmark
10. Common failure modes
11. Production considerations
12. Python vs TypeScript
13. Interview-level architecture discussion
14. Exit criteria
15. Update milestone tracker



To Test / Verify the environment: 
uv run pytest

uv run ruff check .

uv run mypy src

uv run rag-platform

Why uv run?

Even if your .venv isn't activated, uv run ensures the command executes against the project's managed environment.

That's particularly nice for CI/CD later.
